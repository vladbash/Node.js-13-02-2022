doctype html
html(lang="en")
    include header.pug
    body 
        h1 Hello from pug
        span= username
        if items && items.length
            ul
                each item in items
                    li= `[${item.date}] ${item.value}`

        include footer.pug